/**
 * Testing Framework Configuration
 *
 * Centralized config for test credentials and checkpoint mappings.
 */

import testCredentials from './test-credentials.json';
import checkpointMappings from './checkpoint-mappings.json';

export { testCredentials, checkpointMappings };
